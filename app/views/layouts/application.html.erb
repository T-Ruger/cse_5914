<!DOCTYPE html>
<html>
	<style>
		html, body {
			height: 100vh;
			margin: 0;
		}
		.container-fluid {
    	height: 90vh;
    	 /* don't show content that exceeds my height */
		}
	</style>

  <head>
    <title>Movie On Rails</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
		
		<%= Gon::Base.render_data({}) %>
		
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    
  </head>

  <body>
  <%= render partial: 'shared/navigation_bar' %>
    <div div class="container-fluid">
      <div class="row">
        <div class="col-12">
          
          <div class="my-3">
            <%= yield %>
          </div>
        </div>
    </div>
  </body>
</html>
